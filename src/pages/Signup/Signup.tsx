import SignupForm from "../../components/ui/auth/Signup/SignupForm";

function Signup() {

  return (
    <main className={` w-full h-screen flex items-center justify-center theme-bg-primary`}>
      <SignupForm />
    </main>
  );
}

export default Signup;
